/*  REASON:
    Types of arguments of calls must be compatible with the parameters of the method (only for methods of the class being compiled).
*/

class T {
    public static void main(String[] a) {
      io.println(10);
    }

    public int foo(boolean a){
        boolean b;
        b=true;
        this.foo();
        this.foo(true,b);
        return 1;
    }

    public int foo(boolean a, boolean p){
        return 1;
    }
}
