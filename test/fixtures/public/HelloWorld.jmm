import ioPlus;

class HelloWorld {
    public static void main(String[] args) {
        int a;
        int b;
        boolean c;

        if (true && ((1 + 3) < (1 - 3)) && false) {
            b = 1 + 2;
        } else {
            b = 2 + 3;
        }

        b = 2 + this.foo() - 3 / 25 - 10 * (this.foo() * 2);
    }



    public int foo() {
        return 1 + 2;
    }

    public boolean ata(int a) {
        return true;
    }
}