import ioPlus;

class HelloWorld {
    public static void main(String[] args) {
        int a;
        int b;
        boolean c;

        if (true && ((1 + 3) < (1 - 3)) && false) {
            b = 1 + 2;
        } else {
            b = 2 + 3;
        }

        b = this.foo(this.foo(1 - this.foo(1)));
    }

    public int foo(int a) {
        return 1;
    }
}