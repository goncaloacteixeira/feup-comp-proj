import ioPlus;

class HelloWorld {
    public static void main(String[] args) {
        int a;
        int b;
        boolean c;

        a = 2;

        ioPlus.hello();

        a = this.foo(this.foo(this.foo(1 + 2 + this.foo(1 - ioPlus.hi(1 - 2)))));

        a = this.foo(1, 2);
    }

    public int foo(int a) {
        return 1;
    }

    public int foo(int b, int c) {
        return 2;
    }

}